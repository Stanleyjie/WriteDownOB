```SQL
SELECT CULUMNNAME FROM TABLENAME
WHERE 1 = 1
GROUP BY CULUMNNAME
HAVING COUNT(*) > 1; 
```

```SQL
SELECT * FROM TABLENAME
WHERE COLUMNNAME IN (
	SELECT COLUMNNAME FROM TABLENAME
	GROUP BY COLUMNNAME
	HAVING COUNT(*) > 1
)
```

**注意：** WHERE子句必须在GROUP BY之前。